namespace Wasm.Validations
{
    //public class DemoValidation : ValidationAttribute
    //{
    //    protected override ValidationResult IsValid(object value, ValidationContext validationContext)
    //    {
    //        var user = (UserDetailsDto)validationContext.ObjectInstance;

    //        if (user.Email == null || !user.Email.Contains(user.Name))
    //        {
    //            return new ValidationResult("Mail has to contain name", new List<string> { validationContext.MemberName });
    //        }
    //        return ValidationResult.Success;
    //    }

    //}
}
